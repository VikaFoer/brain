# Налаштування бази даних на Railway

## ⚠️ Важливо: SQLite не працює на Railway

SQLite використовує файлову систему, яка на Railway є **тимчасовою**. Дані будуть втрачені при перезапуску контейнера.

## ✅ Рішення: Використайте PostgreSQL

### Варіант 1: Додати PostgreSQL через Railway Dashboard

1. У Railway Dashboard натисніть **"+ New"** → **"Database"** → **"Add PostgreSQL"**
2. Railway автоматично створить змінну `DATABASE_URL`
3. Додаток автоматично підключиться до PostgreSQL

### Варіант 2: Вручну додати DATABASE_URL

Якщо у вас вже є PostgreSQL:

1. Відкрийте Railway Dashboard → Ваш сервіс → **Variables**
2. Додайте змінну:
   - **Name:** `DATABASE_URL`
   - **Value:** `postgresql://user:password@host:port/dbname`

### Перевірка

Після додавання PostgreSQL:

1. Перезапустіть сервіс (Railway зробить це автоматично)
2. Перевірте статус: `https://your-app.railway.app/api/status`
3. Ініціалізуйте категорії: `POST /api/legal-acts/initialize-categories`

## 🔧 Якщо PostgreSQL недоступний

Додаток все одно запуститься, але:
- База даних не працюватиме
- Категорії не можна буде ініціалізувати
- API endpoints повертатимуть помилки

Рекомендується використовувати PostgreSQL для продакшену.
