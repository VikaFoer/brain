# 🔧 Виправлення: Дані зникають після деплою

## ❌ Проблема

Після кожного деплою на Railway всі дані зникають, тому що:
- За замовчуванням використовується **SQLite** (файлова база даних)
- Railway має **тимчасову файлову систему** - файли втрачаються при перезапуску

## ✅ Рішення: Додати PostgreSQL на Railway

### Крок 1: Додати PostgreSQL сервіс

1. Відкрийте **Railway Dashboard**: https://railway.app
2. Виберіть ваш проект
3. Натисніть **"+ New"** (або **"New"** → **"Database"**)
4. Виберіть **"PostgreSQL"**
5. Railway автоматично:
   - Створить PostgreSQL базу даних
   - Додасть змінну `DATABASE_URL` до вашого сервісу
   - Налаштує підключення

### Крок 2: Перевірити змінну DATABASE_URL

1. В Railway Dashboard → Ваш сервіс → **Variables**
2. Перевірте, чи є змінна `DATABASE_URL`
3. Вона має виглядати так: `postgresql://postgres:password@host:port/railway`

### Крок 3: Перезапустити сервіс

Railway автоматично перезапустить сервіс після додавання PostgreSQL.

### Крок 4: Ініціалізувати базу даних

Після перезапуску виконайте:

```bash
# Через curl або Postman
POST https://your-app.railway.app/api/legal-acts/initialize-categories
```

Або через браузер відкрийте:
```
https://your-app.railway.app/api/legal-acts/initialize-categories
```
(Якщо це GET endpoint, просто відкрийте в браузері)

## 🔍 Перевірка

1. Відкрийте: `https://your-app.railway.app/api/status`
2. Перевірте, чи `database_connected: true`
3. Перевірте, чи `database_tables_exist: true`

## 📝 Важливо

- **PostgreSQL на Railway безкоштовний** для початкового використання
- Дані будуть **зберігатися постійно** навіть після деплою
- SQLite **НЕ підходить** для продакшену на Railway

## 🚨 Якщо PostgreSQL вже додано, але дані все одно зникають

1. Перевірте, чи `DATABASE_URL` правильно встановлена в Variables
2. Перевірте логи Railway - чи підключається до PostgreSQL
3. Переконайтеся, що ви ініціалізуєте категорії після кожного деплою (або налаштуйте автоматичну ініціалізацію)

