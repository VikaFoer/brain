# 🚨 Чому дані зникають після деплою

## ❌ Основна причина

**Якщо ви не додали PostgreSQL на Railway**, система використовує SQLite, який:
- Зберігає дані у файлі на диску
- Railway має **тимчасову файлову систему**
- Після кожного перезапуску/деплою файл зникає
- **Всі дані втрачаються**

## ✅ Рішення: PostgreSQL

### Крок 1: Додати PostgreSQL на Railway

1. Відкрийте **Railway Dashboard**: https://railway.app
2. Виберіть ваш проект
3. Натисніть **"+ New"** → **"Database"** → **"Add PostgreSQL"**
4. Railway автоматично:
   - Створить PostgreSQL базу даних
   - Додасть змінну `DATABASE_URL` до вашого сервісу
   - Налаштує підключення

### Крок 2: Перевірити підключення

Після додавання PostgreSQL перевірте:
```
https://your-app.railway.app/api/status
```

Має бути:
```json
{
  "database": {
    "type": "postgresql",  // НЕ "sqlite"!
    "connected": true
  }
}
```

### Крок 3: Автоматична ініціалізація

Після останнього оновлення система **автоматично ініціалізує категорії** при старті, якщо їх немає.

Якщо автоматична ініціалізація не спрацювала, виконайте вручну:
```
https://your-app.railway.app/api/legal-acts/initialize-categories
```

## 🔍 Як перевірити, чи використовується SQLite

1. Відкрийте **Railway Dashboard** → Ваш сервіс → **Variables**
2. Перевірте, чи є змінна `DATABASE_URL`
3. Якщо її немає або вона починається з `sqlite://` - використовується SQLite ❌
4. Якщо вона починається з `postgresql://` - використовується PostgreSQL ✅

## 📝 Важливо

- **PostgreSQL на Railway безкоштовний** для початкового використання
- Дані будуть **зберігатися постійно** навіть після деплою
- SQLite **НЕ підходить** для продакшену на Railway
- Після додавання PostgreSQL дані більше не зникатимуть

## 🚨 Якщо PostgreSQL вже додано, але дані все одно зникають

1. Перевірте логи Railway - чи підключається до PostgreSQL
2. Перевірте `/api/status` - чи `database.type: "postgresql"`
3. Переконайтеся, що `DATABASE_URL` правильно встановлена в Variables
4. Перезапустіть сервіс після додавання PostgreSQL




