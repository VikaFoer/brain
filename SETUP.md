# Інструкція з встановлення та налаштування

## 📋 Вимоги

- Python 3.10+
- PostgreSQL 12+
- Neo4j 5.0+
- Node.js 18+ (опціонально, для frontend)

## 🚀 Крок 1: Встановлення залежностей

```bash
# Встановити Python залежності
pip install -r requirements.txt
```

## 🗄️ Крок 2: Налаштування баз даних

### PostgreSQL

1. Створити базу даних:
```sql
CREATE DATABASE legal_db;
```

2. Оновити `.env` файл:
```
DATABASE_URL=postgresql://user:password@localhost:5432/legal_db
```

### Neo4j

1. Встановити Neo4j Desktop або використати Docker:
```bash
docker run -p 7474:7474 -p 7687:7687 -e NEO4J_AUTH=neo4j/password neo4j:latest
```

2. Оновити `.env` файл:
```
NEO4J_URI=bolt://localhost:7687
NEO4J_USER=neo4j
NEO4J_PASSWORD=password
```

## 🔑 Крок 3: Налаштування API ключів

1. Створити `.env` файл (скопіювати з `.env.example`):
```bash
cp .env.example .env
```

2. Додати ключі:
```
OPENAI_API_KEY=your_openai_key_here
RADA_API_TOKEN=your_rada_token_here  # Опціонально, можна отримати через API
```

### Отримання токену Rada API

Токен можна отримати автоматично через API або вручну:
```python
import requests
response = requests.get("https://data.rada.gov.ua/api/token")
token = response.text
```

## 📊 Крок 4: Створення міграцій та ініціалізація БД

```bash
# Створити міграції
alembic revision --autogenerate -m "Initial migration"

# Застосувати міграції
alembic upgrade head

# Ініціалізувати категорії
python scripts/init_db.py
```

## 🎯 Крок 5: Запуск сервера

```bash
# Запустити FastAPI сервер
uvicorn app.main:app --reload --host 0.0.0.0 --port 8000
```

Сервер буде доступний за адресою: `http://localhost:8000`

API документація: `http://localhost:8000/docs`

## 🌐 Крок 6: Відкрити frontend

Відкрити файл `frontend/index.html` в браузері або налаштувати веб-сервер.

Для локального веб-сервера (Python):
```bash
cd frontend
python -m http.server 8080
```

Потім відкрити: `http://localhost:8080`

## 📝 Використання

### 1. Ініціалізація категорій

Категорії автоматично ініціалізуються при запуску `scripts/init_db.py`.

### 2. Завантаження та обробка актів

```python
# Через API
POST /api/legal-acts/{nreg}/process

# Або через Python
from app.services.processing_service import ProcessingService
from app.core.database import SessionLocal

db = SessionLocal()
service = ProcessingService(db)
await service.process_legal_act("254к/96-вр")  # Конституція
```

### 3. Перегляд графа

1. Відкрити frontend
2. Виберіть категорії (множини)
3. Натисніть "Побудувати граф"
4. Використовуйте чат для аналітики

## 🔧 Налаштування

### Rate Limiting для Rada API

В `.env` можна налаштувати:
```
RADA_API_RATE_LIMIT=60  # запитів на хвилину
RADA_API_DELAY=6.0      # секунд між запитами
```

### OpenAI Model

```
OPENAI_MODEL=gpt-4-turbo-preview  # або gpt-3.5-turbo
```

## ⚠️ Важливо

1. **Не звертайтесь за токеном Rada API перед кожним запитом** - IP буде заблоковано
2. **Дотримуйтесь rate limits** - 60 запитів/хвилину, пауза 5-7 секунд
3. **Neo4j та PostgreSQL повинні бути запущені** перед стартом додатку

## 🐛 Troubleshooting

### Помилка підключення до PostgreSQL
- Перевірте, чи запущений PostgreSQL
- Перевірте правильність `DATABASE_URL` в `.env`

### Помилка підключення до Neo4j
- Перевірте, чи запущений Neo4j
- Перевірте правильність `NEO4J_URI` та пароль в `.env`

### Помилка OpenAI API
- Перевірте правильність `OPENAI_API_KEY`
- Перевірте баланс на рахунку OpenAI

### Помилка Rada API
- Перевірте, чи не перевищено rate limit
- Спробуйте отримати новий токен

