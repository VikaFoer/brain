# Інструкції для завантаження на GitHub

## 🔧 Встановлення Git

Якщо Git не встановлений, завантажте його з: https://git-scm.com/download/win

Після встановлення перезапустіть термінал.

## 🚀 Швидке завантаження

### Варіант 1: Автоматичний скрипт (PowerShell)

```powershell
.\deploy_to_github.ps1
```

### Варіант 2: Вручну

```bash
# 1. Ініціалізація репозиторію (якщо ще не зроблено)
git init

# 2. Додавання remote репозиторію
git remote add origin https://github.com/VikaFoer/brain.git

# 3. Додавання всіх файлів
git add .

# 4. Створення commit
git commit -m "Initial commit: Legal Graph System"

# 5. Встановлення гілки main
git branch -M main

# 6. Завантаження на GitHub
git push -u origin main
```

## ⚠️ Якщо репозиторій вже існує на GitHub

Якщо репозиторій вже створений на GitHub, але порожній:

```bash
git pull origin main --allow-unrelated-histories
```

Потім знову:
```bash
git push -u origin main
```

## 🔐 Автентифікація

GitHub може вимагати автентифікацію. Варіанти:

1. **Personal Access Token** (рекомендовано):
   - Створіть токен: https://github.com/settings/tokens
   - Використовуйте токен замість пароля

2. **GitHub CLI**:
   ```bash
   gh auth login
   ```

3. **SSH ключ**:
   ```bash
   git remote set-url origin git@github.com:VikaFoer/brain.git
   ```

## 📝 Наступні оновлення

Після першого завантаження, для оновлень:

```bash
git add .
git commit -m "Опис змін"
git push
```

