# 🔗 Як підключити PostgreSQL до сервісу з додатком

## ⚠️ Проблема

Якщо PostgreSQL додано, але дані все одно зникають, це означає, що:
- PostgreSQL сервіс існує ✅
- Але змінна `DATABASE_URL` не підключена до сервісу з додатком ❌

## ✅ Рішення: Підключити DATABASE_URL

### Варіант 1: Через Railway Dashboard (найпростіше)

1. Відкрийте **Railway Dashboard** → Ваш проект
2. Виберіть **сервіс з додатком** (не PostgreSQL, а той де запускається Python)
3. Перейдіть на вкладку **"Variables"**
4. Натисніть **"+ New Variable"**
5. В полі **"Name"** введіть: `DATABASE_URL`
6. В полі **"Value"** натисніть на іконку **🔗 (Reference)** поруч з полем
7. Виберіть:
   - **Service:** `Postgres` (або назва вашого PostgreSQL сервісу)
   - **Variable:** `DATABASE_URL`
8. Натисніть **"Add"**

Railway автоматично підключить змінну `DATABASE_URL` від PostgreSQL сервісу до вашого додатку.

### Варіант 2: Вручну скопіювати DATABASE_URL

1. Відкрийте **Railway Dashboard** → Ваш проект
2. Виберіть **PostgreSQL сервіс**
3. Перейдіть на вкладку **"Variables"**
4. Знайдіть змінну `DATABASE_URL` та скопіюйте її значення
5. Перейдіть до **сервісу з додатком** → **"Variables"**
6. Натисніть **"+ New Variable"**
7. Введіть:
   - **Name:** `DATABASE_URL`
   - **Value:** (вставте скопійоване значення)
8. Натисніть **"Add"**

## 🔍 Перевірка після підключення

Після додавання змінної:

1. Railway автоматично перезапустить сервіс
2. Перевірте статус:
   ```
   https://your-app.railway.app/api/status
   ```
3. Має бути:
   ```json
   {
     "database": {
       "type": "postgresql",  // НЕ "sqlite"!
       "connected": true
     }
   }
   ```

## 📝 Важливо

- Змінна `DATABASE_URL` має бути в **сервісі з додатком**, а не тільки в PostgreSQL сервісі
- Railway автоматично перезапустить сервіс після додавання змінної
- Після перезапуску категорії ініціалізуються автоматично

## 🚨 Якщо все одно не працює

1. Перевірте логи Railway → Deploy Logs
2. Шукайте повідомлення:
   - `✔ Using PostgreSQL database (persistent)` - правильно ✅
   - `⚠️ WARNING: SQLite will lose data` - неправильно ❌
3. Перевірте, чи змінна `DATABASE_URL` дійсно додана в сервіс з додатком

