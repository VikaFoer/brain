# 🚀 Швидкий старт

## Для завантаження на GitHub:

### 1. Встановіть Git (якщо ще не встановлено)
Завантажте з: https://git-scm.com/download/win

### 2. Запустіть скрипт
```powershell
.\deploy_to_github.ps1
```

Або виконайте команди вручну (див. GIT_INSTRUCTIONS.md)

---

## Для запуску проекту:

1. Встановіть залежності: `pip install -r requirements.txt`
2. Налаштуйте `.env` (скопіюйте `.env.example`)
3. Запустіть PostgreSQL та Neo4j
4. Застосуйте міграції: `alembic upgrade head`
5. Ініціалізуйте категорії: `python scripts/init_db.py`
6. Запустіть: `python run.py`

Детальніше: [SETUP.md](SETUP.md)

